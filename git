git init
git remote add origin https://github.com/rba1aji/bus-details-system-web-api

git add .
git commit -m 'msg'
git push -u origin master

git pull

git log
git status

git fetch

git rm -r --cached build

git checkout 7292d82b3 .
